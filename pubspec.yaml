name: pms_app
description: "HRMS Employee Mobile App"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.3.2
  provider: ^6.1.2
  google_fonts: ^8.0.2
  intl: ^0.20.2
  geolocator: ^14.0.2
  file_picker: ^10.3.10
  http_parser: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  change_app_package_name: ^1.5.0

flutter:
  uses-material-design: true
